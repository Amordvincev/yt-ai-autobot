name: Daily AI Video
on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: sudo apt update && sudo apt install -y ffmpeg espeak fonts-noto
      - run: pip install -r requirements.txt
      - run: mkdir -p assets output .yt_token
      - run: echo '${{ secrets.YT_CLIENT_SECRET }}' > client_secret.json
      - run: python -c "import base64,os; os.makedirs('.yt_token',exist_ok=True); open('.yt_token/token.pickle','wb').write(base64.b64decode('${{ secrets.YT_TOKEN }}'))"
      - run: python main.py
      - uses: actions/upload-artifact@v4
        with:
          name: video
          path: output/*.mp4
